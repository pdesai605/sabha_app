import { VisitorRegister } from "@/modules/visitor-desk/components/visitor-register";

export default function VisitorRegisterPage() {
  return (
    <VisitorRegister
      breadcrumbExtra={{ label: "Visitor Register" }}
    />
  );
}
